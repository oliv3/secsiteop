%% window size
-define(WIDTH,   640).
-define(HEIGHT,  480).

%% # of samples per host
-define(MAXDATA, 10). %% FIXME 30 does not work, for example

%% maximum load average
-define(CLAMP,   2.5).

%% misc shortcuts
-define(ZERO,    0.0).
-define(ONE,     1.0).
